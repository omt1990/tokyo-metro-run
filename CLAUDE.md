# 東京メトロラン — セッションの入口

⚠️ **このリポジトリは PUBLIC です。** 個人情報・他プロジェクトの内部事情・参加者の氏名を書かないこと。

## 最初に読む

| ファイル | 中身 |
|---|---|
| **`DOCS.md`** | 仕様と設計の正本（ゲームルール／技術スタック／データモデル／デプロイ手順） |
| **`docs/product-mission.md`** | ★ **このプロダクトが何のためにあるか。**機能の優先度を決めるときはここに戻る |

## 用語の注意

★ 「**ミッション**」はこのプロダクトで**2つの意味**を持つ。混同しないこと。

| 語 | 意味 | 場所 |
|---|---|---|
| **ミッション（ゲーム内）** | プレイヤーが達成するお題。ミッションビンゴのマス | `DOCS.md` §1, §4 |
| **プロダクトミッション** | このプロダクトの存在理由 | `docs/product-mission.md` |

## 現在のフェーズ

**仲間内で遊ぶ MVP。** 将来的に一般化・アプリ化したい、という位置づけ。
→ 機能を足すか迷ったら `docs/product-mission.md` の「現フェーズの判断基準」を見る。

## 受容済みリスク（2026-09-06 全体監査 B-10）

`database.rules.json` は `games_v6/$code` を `auth != null` で読み書き許可している。
匿名認証を有効にしてあるため、**Firebase に匿名サインインできる人なら誰でも、
6桁コードを総当たりして他人のゲーム状態を読み書きできる**。

これを直さないと決めた理由：格納しているのは友人同士で遊ぶ一時的なゲーム状態だけで、
個人情報も課金もなく、荒らされても新しい部屋を作り直せば済むため。
直すとしたら「部屋の作成者 uid を持たせて書き込みを本人に限る」または「匿名認証を切る」。

`index.html` の Firebase Web API キーが公開されている件は**別の話で、こちらは問題ない**。
Firebase の Web API キーは公開前提の識別子であり、アクセス制御はルール側が担う設計。
週次スキャンの警告は `audits/.secscan-baseline.txt` に登録して止めた（2026-09-06）。
